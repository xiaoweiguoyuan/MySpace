package com.wuba.car.myspace.enum

enum class PattleEnum(i: Int) {
    darkMutedColor(0),
    darkVibrantColor(1),
    lightVibrantColor(2),
    lightMutedColor(3),
    mutedColor(4),
    vibrantColor(5),
}